#pragma once
#include <limits>
#include <iostream>

// Clear invalid input from the standard input stream
// This prevents infinite loops when the user enters non-numeric input
void clearInput();
